# Local secrets — copy to .env.local (never commit real keys)

# Google Gemini — enables real AI enrich on URL/text import
# Create a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional model override (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Shared kitchen on Vercel (GitHub-backed store)
# GITHUB_TOKEN=
# GITHUB_REPO=ValentynPi/oselya-kitchen
# GITHUB_BRANCH=main
